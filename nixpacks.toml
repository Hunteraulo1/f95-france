[phases.setup]
nixPkgs = ["nodejs_24", "bun"]

[phases.install]
cmds = [
  "rm -rf node_modules bun.lockb || true",
  "bun install --production"
]

[phases.build]
cmds = [
  "bun run build"
]

[start]
cmd = "bun run start"
