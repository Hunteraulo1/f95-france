[phases.setup]
# Client PostgreSQL pour sync prod → PTB au démarrage (pg_dump / psql).
nixPkgs = ["nodejs_24", "bun", "postgresql"]

[phases.install]
cmds = [
  "rm -rf node_modules bun.lockb || true",
  "bun install"
]

[phases.build]
cmds = [
  "bun run build"
]

[start]
cmd = "bun run start"
