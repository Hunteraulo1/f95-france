[phases.setup]
# Client MariaDB pour sync prod → PTB au démarrage (mysqldump / mysql).
nixPkgs = ["nodejs_24", "bun", "mariadb"]

[phases.install]
cmds = [
  "rm -rf node_modules bun.lockb || true",
  "bun install"
]

[phases.build]
cmds = [
  "bun run build"
]

[start]
cmd = "bun run start"
