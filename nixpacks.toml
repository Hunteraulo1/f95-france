[phases.setup]
nixPkgs = ["nodejs_24", "bun"]

[phases.install]
cmds = [
  "rm -rf node_modules bun.lockb || true",
  "bun install",
  "bun run db:migrate"
]

[phases.build]
cmds = [
  "bun run build"
]

[start]
cmd = "node build/handler.js"
